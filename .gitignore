bin/
build/
generated/
rom/*
cfg/*
asm/*
fm2/*
!rom/.gitkeep
!cfg/.gitkeep
!cfg/game.cfg.example
!asm/.gitkeep
!fm2/.gitkeep
*.png
!docs/assets/*.png
*.ppm
*.log
*.cfg
.groupedtimelineinclude
.history/
.vscode-ctags
__pycache__/

# GPL FCEUX vendor oracle — kept local only so the tree builds & commits GPL-free.
# Build auto-detects it (Makefile VENDOR_SRCS); absent → --interp=fceux_vendor off.
/nogpl/

# external tool symlinks / binaries (absolute paths, not portable)
/Mesen
/fceux
/SDL2.dll

# temporary working files
/tmp/
/tools/fceux_sync.txt

# generated lag/sync dumps — regenerate with tools/compare_lags.sh
/lags/

# docs working material — not tracked
/docs/nesdev/

# mesen-format player (unused, possibly stale)
/src/mmo_player.c
/src/include/mmo_player.h
